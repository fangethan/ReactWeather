var React = require("react");

var WeatherMessage = React.createClass({
    render() {
        return (
            <div>
                <h2>WeatherMessage page</h2>     

            </div>
        );
    }
});

module.exports = WeatherMessage;
